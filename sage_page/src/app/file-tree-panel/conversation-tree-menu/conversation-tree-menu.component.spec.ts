import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConversationTreeMenuComponent } from './conversation-tree-menu.component';

describe('ConversationTreeMenuComponent', () => {
  let component: ConversationTreeMenuComponent;
  let fixture: ComponentFixture<ConversationTreeMenuComponent>;

  beforeEach(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ConversationTreeMenuComponent]
    // });
    // fixture = TestBed.createComponent(ConversationTreeMenuComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
