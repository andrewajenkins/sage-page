import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConversationTreeComponent } from './conversation-tree.component';

describe('ConversationTreeComponent', () => {
  let component: ConversationTreeComponent;
  let fixture: ComponentFixture<ConversationTreeComponent>;

  beforeEach(() => {
    // TestBed.configureTestingModule({
    //   declarations: [ConversationTreeComponent]
    // });
    // fixture = TestBed.createComponent(ConversationTreeComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
